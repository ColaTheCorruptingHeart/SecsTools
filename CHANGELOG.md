# 更新日志

本文档记录 SECS Tools 的重要功能变化。尚未发布的改动统一收录在“未发布”章节，版本号与发布日期以 `package.json` 和正式 Release 为准。

## 未发布

暂无。

## 0.3.2 - 2026-09-23

### SML 构造器

- 新增可视化 SML 构造器，支持通过结构树编辑节点、类型、标签和值，并自动维护 List 项数。
- 内置覆盖通信、状态、设备常量、事件、报警、配方及协议错误等常用场景的 GEM 报文模板。
- 支持从模板载入、从 SML 源码应用、严格模式校验、ASCII/JIS8 声明长度以及文本与数值引号策略。
- 增强节点复制粘贴、缩进、移动、折叠等上下文操作，并统一构造器与日志时间线的右键菜单样式和图标。
- SML 构造器与格式化工具新增长度标识显示选项，解析器同步保留引号内空白与字符长度信息。

### 日志时间线与导出

- 优化消息块悬停、文本拖选和时间线定位的视觉层级，使消息块高亮与文本选区能够同时清晰显示。
- 日志规则导出改为设备号、自动时间戳和内容哈希命名，并支持设备号本地保存、复用及删除。
- 标记区间、命中报文和报文集继续使用结构化文件名，统一复用机台号历史记录和内容哈希校验信息。
- 日志时间线和 SML 构造器共享 Element Plus 风格的自定义右键菜单，时间线菜单补充对应操作图标。

### S1F12 SVID 提取

- CSV 导出新增自定义命名弹窗，仅需填写设备号，自动生成时间戳和内容哈希。
- 设备号与日志导出共享本地历史记录，支持直接复用和删除。
- 桌面端原始报文与提取结果区域调整为 4:6，占用更多空间展示 SVID、SVNAME、UNITS 和备注。

### 视觉与交互

- 全站以 Element Plus 语义色为界面色源，Tailwind CSS 负责布局及语义类映射，统一页面、弹窗、侧栏、卡片和控件状态。
- 保留 SML 数据类型色卡、日志规则颜色和差异标记等业务色，避免统一主题后削弱数据辨识度。
- 对齐 SML 构造器与 S1F3 生成器的页边距、标题栏和面板间距，并修复数据类型选择器显示不完整的问题。

### 文档

- 重写项目说明，补充工具清单、浏览器本地处理、开发测试和静态部署说明。
- 更新贡献指南、安全策略与 SML 解析器文档，统一默认分支、命令和解析器契约。

## 0.3.1 - 2026-09-16

### SML 解析与格式化

- SECS SML 格式化、日志时间线、语义差异和 S1F12 提取现已共用统一解析能力。
- 扩展对类型别名、计数写法、字段标签、注释、报文终止符、多根节点及不同日志包裹格式的兼容。
- 格式化器新增宽松与严格模式，并提供可定位到原文的错误、警告诊断。

### 日志时间线

- 新增自定义 CEID 匹配，可配置 Stream、Function 和关键值路径，并可随规则配置导入、导出。
- 范围起点与终点现在会直接显示在时间线中。
- 单次日志导入上限由 50 MiB、30 万行提升至 100 MiB、90 万行。
- 命中报文与报文集导出统一使用机台号、批次号或导出类型、日志日期及内容哈希命名，并复用机台号历史记录。

### S1F12 与稳定性

- S1F12 SVID 提取新增结构化解析诊断，支持点击诊断定位源码。
- 增强异常输入恢复、日志方言兼容及大文件处理可靠性。
- 增加 SML 消费链路、日志解析、范围标记及核心页面的自动化测试覆盖。

### Slot 转换

- 新增有片与空槽的自定义单数字映射，默认有片为 `1`、空槽为 `0`，且两个映射值不能相同。
- 25 位 map 与反相 map 支持按当前映射生成及反向恢复槽位。
- 新增可编辑的 25 项纵向 `<L><U1 …>` List 及其反相视图，支持复制、粘贴，并在严格 SML 格式化和校验后应用到槽位。
