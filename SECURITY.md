# 安全策略

## 支持范围

项目仍在快速演进，目前仅对默认分支 `main` 上的最新代码提供安全修复。历史提交、个人 Fork 和第三方部署由其维护者自行评估并升级。

| 版本 | 安全更新 |
| --- | --- |
| `main` 最新代码 | 支持 |
| 历史版本或旧提交 | 不保证 |

## 关注的问题范围

SECS Tools 是在浏览器中处理用户输入的纯前端应用。以下问题属于本安全策略的关注范围：

- 构造后的报文、日志或文件内容可触发跨站脚本（XSS）或其他非预期代码执行
- 特制输入可绕过既有资源限制，导致明显的内存耗尽或拒绝服务
- PWA、静态资源缓存或构建配置可能造成内容被错误替换或长期使用易受攻击的版本
- 依赖、构建脚本或发布流程中的供应链风险
- 应当留在浏览器本地的数据被意外发送到外部服务

一般功能缺陷、解析结果不符合预期或性能优化建议，请使用普通 Issue 模板报告；如果缺陷可能影响数据机密性、完整性或运行环境安全，则按下述流程私密报告。

## 报告安全问题

如果你发现了可能影响用户、部署环境或依赖供应链的安全问题，请不要直接公开提交包含完整利用细节的 Issue。

建议按以下顺序处理：

1. 如果仓库已启用 GitHub Private Vulnerability Reporting，请通过仓库的 **Security → Report a vulnerability** 页面提交。
2. 如果仓库尚未启用私密报告，可通过仓库所有者公开的 GitHub 联系方式私下联系。
3. 如果暂时没有私密联系渠道，请仅创建一个不包含利用细节的公开 Issue，标题可注明“security concern”，并说明需要私下沟通。

报告时请尽量包含：

- 问题影响范围
- 受影响的版本、提交或部署方式
- 触发条件与复现步骤
- 已脱敏的最小样例或概念验证
- 可能的风险等级和已知缓解措施
- 建议的修复方向（如果有）

## 处理预期

- 维护者会尽量在 7 个自然日内完成首次响应。
- 如果问题被确认，会根据影响范围安排修复与发布。
- 修复时间取决于问题复杂度和发布风险，确认问题后会通过私密渠道同步进展。
- 在修复发布前，请不要公开披露可直接复现或利用的细节。

## 敏感数据

请勿公开提交：

- 可直接复现的攻击步骤或未修复漏洞的完整利用代码
- 含有密钥、令牌、账号、客户信息或设备标识的日志
- 真实生产环境中的敏感配置、报文、RecipeBody 或其他数据样本

报告前请使用虚构或最小化数据复现问题。即使应用默认在浏览器本地处理数据，也不应把真实生产数据上传到 Issue、讨论区或 Pull Request。

感谢你以负责任的方式帮助改进本项目的安全性。
