# 贡献指南

感谢你关注 SECS Tools。

本项目欢迎针对真实工程场景的改进，包括功能增强、缺陷修复、文档完善、性能优化和测试样例补充。开始贡献前，请先阅读 [README](README.md) 了解项目定位；如果改动涉及统一 SML 解析器，还需要阅读 [SML 解析器说明](docs/sml-parser.md)。

## 贡献方向

优先欢迎以下类型的贡献：

- SECS/HSMS 报文处理与解析能力增强
- 日志时间线和语义差异分析的准确率、性能与可读性优化
- RecipeBody、SVID、SML 等工具的边界场景补充
- 面向现场排障的交互改进、帮助说明和错误提示优化
- 与现有工具风格一致的新增小工具

以下改动请先通过 Issue 讨论：

- 与仓库目标无关的大型功能扩张
- 明显偏一次性脚本性质的需求
- 缺少真实使用场景支撑的大范围重构
- 新增网络请求、遥测、账号体系或外部服务依赖

## 准备开发环境

- Node.js `^20.19.0` 或 `>=22.12.0`
- npm 10 或更高版本

克隆仓库并安装依赖：

```bash
git clone https://github.com/ColaTheCorruptingHeart/SecsTools.git
cd SecsTools
npm install
```

启动开发环境：

```bash
npm run dev
```

开发服务器默认运行在 <http://localhost:63897>。

## 代码结构约定

- 工具入口统一在 `src/config/tools.ts` 注册，页面组件放在 `src/views/tools/`；路由与导航会根据配置自动生成。
- 可复用的领域逻辑应从 Vue 组件中拆出，以便单元测试和其他工具复用。
- 解析大日志、配方体或复杂报文时，优先使用 Web Worker，避免长时间阻塞 UI 线程。
- SML 相关功能优先复用 `src/views/tools/secsSml.ts` 暴露的统一解析和格式化接口，不要在页面内另写一套解析器。
- 用户输入默认只在浏览器内处理。新增网络请求、遥测或外部服务依赖前，必须先说明用途和数据边界。

## 验证改动

提交前至少运行：

```bash
npm run build
npm run lint
npm test
```

`npm run lint` 会自动修复可修复的问题，运行前请确认工作区中的改动已经保存。涉及页面交互或关键流程时，还应运行：

```bash
npm run test:e2e
```

首次运行 Playwright 测试可能需要安装浏览器：

```bash
npx playwright install chromium
```

测试应与改动风险匹配：

| 改动类型 | 建议验证 |
| --- | --- |
| 纯文档 | 检查链接、命令和 `git diff --check` |
| 解析器或数据转换 | 补充 Vitest 单元/集成测试，并运行 `npm test` |
| Vue 页面交互 | 运行相关 Vitest 测试，必要时补充 Playwright 用例 |
| 构建、依赖或 PWA | 运行 `npm run build`，并检查生产预览 |

## 提交流程

1. 确认仓库中没有相同主题的 Issue 或 Pull Request。
2. 对于较大的改动，先开 Issue 说明背景、目标和边界。
3. 从 `main` 拉出新分支进行开发。
4. 完成修改后执行与改动范围匹配的构建、检查和测试。
5. 提交 Pull Request，并说明改动内容、验证方式和潜在影响。

## Pull Request 要求

- 单个 PR 聚焦一个问题，不混入无关重构。
- 保持与现有代码风格一致。
- 新增功能附带必要的说明、样例或测试输入。
- 修改日志或协议解析时，提供脱敏后的最小可复现样本，并覆盖正常输入与错误输入。
- 用户可见的功能变化同步更新 README、相关领域文档和 `CHANGELOG.md` 的“未发布”章节。
- 不要提交真实生产环境中的敏感日志、设备标识、账号或客户数据。

## 代码与文档约定

- 优先修复根因，不做表面修补，也不要顺手修改无关问题。
- 文档、文案和 UI 文本默认使用中文；必要时可补充英文术语。
- 文件名、命令、类型名和代码标识符使用反引号标记。
- 文档中的功能、限制和示例必须与当前代码一致，不描述尚未实现的能力。

## 问题反馈建议

报告缺陷时，请尽量提供：

- 使用的工具页面
- 实际结果与预期结果
- 浏览器、Node.js 或部署环境信息
- 可复现问题的最小脱敏输入

如果问题涉及安全风险，请不要直接公开完整利用细节，请按照 [安全策略](SECURITY.md) 中的流程报告。

感谢你的贡献。
